# shared

Monkey C used by every face in this repo. Each face's `monkey.jungle`
adds this folder:

```
base.sourcePath = source;../../shared/source
```

Put analog primitives here (`AnalogGeometry`). Keep face layout, color,
and numerals in `faces/<name>/source`.
